name: Release Branch CI

on:
  push:
    branches:
      - release

jobs:
  install_flutter_sdk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Check Flutter
        run: flutter --version

  build:
    needs: install_flutter_sdk
    runs-on: ubuntu-latest

    steps:
      - name: Get Pub dependencies
        run: flutter pub get